We are currently working somewhat separately to implement all the API calls. To run the tests we have currently for the Lab 4 demo:

1. Make sure you have .env file set up with the relevant credentials
2. Run ```python angela_tests.py``` and ```python william_tests.py ```
   
   ** There may be some overlap with angela_tests and william_tests in our effort to consolidate them - still a work in progress.